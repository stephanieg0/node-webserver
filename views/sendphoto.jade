extends ./layout.jade

block content
  .container
    h1 Send us your favorite photos!

    form(method='post', enctype='multipart/form-data')
      input(type='file', name='image', accept="image/*")
      input(type='submit', value='Share now')

